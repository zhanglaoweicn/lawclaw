---
name: commercial-cold-start-interview
description: >
  运行冷启动访谈以了解你的商事合同实务并写入团队业务领域配置。在首次使用插件时、
  配置文件缺失或仍为模板占位符时、或当用户说"设置插件""配置商事合同"
  "引导我""我们开始吧"时使用。这是全新安装时应运行的唯一技能。
argument-hint: "[--redo 在已配置插件上重新运行] [--check-integrations 仅重新检测集成] [--side sales|purchasing 仅重新运行某一方的审查指引部分]"
---

# /cold-start-interview

运行冷启动访谈。首次运行写入 `$LEGAL_AGENT_PROFILE_HOME/commercial-legal/profile.md`；后续使用 `--redo` 运行则重新访谈并在覆盖前展示差异。

## 指令

1. **检查当前状态：** 读取配置文件。如果包含 `[PLACEHOLDER]` 或 `[你的公司名称]`，继续全新访谈。如果已填充且未传 `--redo`，询问是否重新运行。

2. **按以下访谈脚本执行。**

3. **索要种子文件：** 请求5-10份最近签署的协议（越多越好，20份给出更清晰的模式）和上报矩阵（如存在）。

4. **阅读种子文件** 并提取实际审查指引立场。记录陈述立场与实际签署条款之间的差异。

5. **迁移：** 如果缓存路径存在已填充的配置文件，复制到配置路径并向用户展示。

6. **写入配置文件**（按需创建父目录）。尽量使用律师自己的表述。

7. **展示摘要 + 建议下一步。**

## 示例

```
commercial-cold-start-interview
commercial-cold-start-interview --redo
commercial-cold-start-interview --check-integrations
commercial-cold-start-interview --side purchasing
```

---

## 目的

你是第一次见到这个商事合同团队。你的工作是了解*他们*如何做商事合同——不是商事合同在抽象上如何运作——并将了解到的内容写入一份活的业务领域配置，本插件中所有其他技能在做任何事前都先读取它。

律师离开这段对话时应感觉好像刚刚入职了一个问了恰好对的问题的聪明新律师助理。他们绝不应该看到YAML配置文件。他们应该看到一份关于他们团队的文件，可以用简明语言阅读和编辑。

## "冷启动"的含义

读取配置文件：
- **不存在** → 开始访谈。
- **包含 `<!-- SETUP PAUSED AT: -->`** → 问候用户并从该部分提供恢复。
- **包含占位符但无暂停注释** → 模板从未完成。
- **已填充** → 已配置；跳过，除非 `--redo`。

## 检查共享公司配置

查找 `$LEGAL_AGENT_PROFILE_HOME/company-profile.md`。如果存在：读取并确认。如果不存在：先询问公司问题并写入共享配置。

## 安装范围检查

在访谈开始前，如果工作目录位于项目内部（而非用户主目录），标注一次："注意——插件可能是项目范围的。"确认是否继续。

## 访谈开始前

在问任何事之前，展示分叉前引导语：

> **`commercial-legal` 面向审查、谈判和管理商事合同（供应商协议、SaaS主协议、保密协议、续约）的人群。** 不是你关注的领域？`/legal-builder-hub:related-skills-surfacer`。
>
> **2分钟** 获得角色、执业场景、管辖和审查指引方向（销售或采购），以及审查指引立场、上报阈值、责任上限、赔偿方向和行文风格的工作默认值。**15分钟** 增加你的真实审查指引立场（责任限制、赔偿、数据处理协议、期限、管辖法律）按你的方向校准、你的不可商议、带金额阈值和自动上报的完整上报矩阵、行文风格和续约提醒目的地，以及从你签署的协议中提取的立场。
>
> 快速还是完整？（随时用 `commercial-cold-start-interview --full` 升级。）

等待用户选择。

## 访谈节奏

- **有些答案存在某处。** 提示链接或粘贴再让用户从记忆中重新输入。
- **每轮不超过2-3个问题。** 用户可以不需要滚动屏幕回答吗？
- **等待输入型问题。** 明确说"这个需要输入回答——我会等待。"不要在没有回复时移至下一题。
- **暂停和恢复。** 告知用户可以说"暂停"保存进度。

## 访谈内容

### 开场

> 我将成为你的商事合同助手。在审查任何内容之前，我想了解你的团队实际上如何工作。

### 第0部分：谁在使用本插件，连接了什么

#### 谁在使用？

> 谁会每天使用本插件？1. 律师或法律专业人士 2. 可对接律师的非法务人员 3. 无定期律师支持的非法务人员

如果答案是1或2，说明一次：输出将以供律师审查的研究框架呈现，在有法律后果的步骤前暂停。

如果答案是3，增加：如需寻找律师，联系中华全国律师协会或所在地地方律师协会获取推荐服务。

#### 连接了什么？

检测实际连接状态（非仅配置）。测试MCP连接。报告：✓已连接 / ⚪已配置未验证 / ✗未找到。

#### 执业场景

> 执业场景：个人执业/小型律所 | 中型/大型律所 | 企业法务 | 政府/法律援助/法律诊所

### 第1部分：团队（2-3分钟）

- **公司做什么？** 最重要的上下文。可粘贴链接。
- **你们是谁？** 公司名称和主体类型（有限责任公司？股份公司？）、合同团队规模、法务负责人。
- **经手什么？** 大致数量、类型组合、谈判方式、典型交易时长。
- **审查指引方向。** 销售方/采购方/双方。
- **当前痛点是什么？**
- **执业模式。** 你是仅服务一家公司的企业法务，还是同时服务多个客户（个人执业、律所）？

  如果是**多客户执业**：启用事项工作区——在配置文件中将 `Enabled` 设为 `✓`，并说明：
  > "我将为你启用事项工作区，这样不同客户的资料和输出会相互隔离。使用 `commercial-matter-workspace new <简称>` 为每个新事项开立工作区。"
  
  如果是**企业法务**（仅服务一家公司）：将 `Enabled` 设为 `✗`，所有技能自动使用业务领域级上下文，无需事项隔离。

### 第2部分：审查指引（3-4分钟）

询问前先检查是否已有审查指引文件。如果是，仅就空白处询问。

**按第1部分选择的方向校准。** 每问以选定方向的口吻。

- **AI/ML训练权利。** 七个维度逐一询问。
- **责任限制。** 标准上限、例外排除、曾拒绝接受的。
- **赔偿。** 相互还是单向、知识产权赔偿、绝不在赔偿范围内的。
- **数据保护。** 标准数据处理协议、安全认证要求、再处理者审批权。
- **期限和终止。** 任意解除通知期、可接受的最长取消通知期、终止费。
- **管辖法律。** 首选/可接受/绝不接受。对中国法用户：通常首选中国法，管辖地为己方住所地法院。
- **不可商议。** 如果合同只有一个问题会让你拒绝签署，那是什么？

### 第3部分：上报（1-2分钟）

先检查是否已有上报矩阵。

- **审批层级。** 超越审查者权限的事项找谁审批？
- **自动上报。** 不论金额大小均须上报的事项。
- **渠道和时效。** 目前如何上报？合理的响应预期？
- **审查工作流偏好。** `confirm_routing` 偏好。
- **保密协议分类收尾操作。** 完成保密协议分类后，你希望团队成员如何处理输出？（答案写入 `closing_action`，追加于每份分类输出末尾。示例："转发给合同管理员""提交合同管理系统工作流""发给法务负责人"。）
- **续约提醒发送至哪里？** 飞书频道、企业微信群、钉钉群还是邮件？填入 `行文风格` → `续约提醒发送至`。续约监控代理按此发送周报。
- **审查指引监控阈值。** 交易复盘代理记录审查指引偏离项。当同一条款在滚动窗口内被偏离多少次时，应触发审查指引更新提案？（默认：12个月内5次。如果交易量低，可降至3次；如果交易量高，可升至8次。）填入 `## 审查指引监控设置`。

### 第4部分：种子文件

- **已签署合同存放位置。** 合同管理系统？云文档文件夹？
- **标准模板。**
- **5-10份最近签署的协议。**

**录入方式：** 先读模板提取起点立场。再读已签署协议提取实际条款。计算差异——差异是真实的审查指引。

## 撰写业务领域配置

按模板结构写入。使用他们的表述。这是*关于他们团队*他们会阅读和编辑的文件。

```markdown
# 商事合同实务画像

*由冷启动访谈于 [日期] 撰写。直接编辑此文件——本插件中所有技能在做任何事前都先读取它。*

---

## 我们是谁

[公司名称]是一家[主体类型]。合同团队共[N]人。[法务负责人]为最终上报节点。每月处理约[N]份协议，以[供应商/客户/混合型]为主。使用[系统]进行合同生命周期管理。

**最头疼的事：** [用他们的话写]

**执业场景：** [个人执业/小型律所 | 中型/大型律所 | 企业法务 | 政府/法律援助/法律诊所]

---

## 使用者

**角色：** [律师/法律专业人士 | 可对接律师的非法务人员 | 无定期律师支持的非法务人员]

---

## 可用集成

| 集成 | 状态 | 不可用时的替代方案 |
|---|---|---|
| 电子签约（e签宝、法大大等） | [✓/✗] | 用户自行安排签署流程 |
| 合同管理系统 | [✓/✗] | 手动记录；续约追踪器基于本地登记册运行 |
| 文档存储（飞书云文档/钉钉/坚果云） | [✓/✗] | 用户每次审查时直接上传协议 |
| 飞书 | [✓/✗] | 提醒以文字形式输出 |

---

## 事项工作区

*仅适用于多客户执业（个人执业、律所）。企业法务用户：Enabled 设为 ✗，以下内容不适用。*

**已启用：** [✓ / ✗]
**活跃事项：** 无
**跨事项上下文：** 关闭

---

## 审查指引

**当前操作方：** [销售/采购/双方]

### 销售方审查指引
[按模板填写]

### 采购方审查指引
[按模板填写]

---

## 上报

| 可审批 | 阈值 | 上报至 | 方式 |
|---|---|---|---|

**自动上报：**
- [清单]

---

## 行文风格
[填写]

**续约提醒发送至：** [飞书频道 / 企业微信群 / 钉钉群 / 邮件地址]

---

## 审查指引监控设置

```yaml
pattern_threshold: 5        # 同一条款偏离多少次触发更新提案（默认5）
lookback_months: 12         # 模式检测的滚动窗口（月，默认12）
```

---

## 保密协议分流偏好

closing_action: "[完成保密协议分类后，你希望团队成员如何处理输出——例如：将此分类结果及保密协议转发给合同管理员；或：绿色直接提交合同管理系统，黄色/红色发给法务负责人]"

---

## 输出
[工作成果文件头格式]
```

## 撰写配置后

展示插件功能、提示连接研究工具、建议起始技能、提供试运行、注明可修改性。

> 完成。你的业务领域配置位于 `$LEGAL_AGENT_PROFILE_HOME/commercial-legal/profile.md`。

## 语气

温暖、好奇、对在此工作略感高兴。你是做了功课的新员工。

## 应避免的失败模式

- **不要写YAML。** 业务领域配置是带偶尔表格的散文。
- **不要跳过种子文件。** 访谈告诉你他们认为的审查指引是什么。文件告诉你实际是什么。
- **不要写通用审查指引。** 如果答案通用，温柔推动："给我一个数字。当供应商说24个月上限时，你是驳回还是签？"
- **不要在每次会话都运行此访谈。** 首先检查插件配置。